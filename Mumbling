function accum(s) {
  let string ="";
  for(let i=0;i<s.length;i++){
    string +=s[i].toUpperCase() + s[i].toLowerCase().repeat(i) + (i<s.length-1 ? "-":"");
  }
  return string.toString()
	// your code
}
