function catchSignChange(arr){
  let sign_change=0;
  for(let i=0;i<=arr.length;i++){
    if((arr[i]<0 && arr[i+1]>=0) || (arr[i]>=0 && arr[i+1]<0)){
      sign_change+=1;
    }
  }
  return sign_change
}
